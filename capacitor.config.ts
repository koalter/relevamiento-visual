import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.relevamientoVisual.lcea',
  appName: 'relevamiento-visual',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
